[build]
  command = "npm run build"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"

# SPA routing — serve index.html for all paths
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
